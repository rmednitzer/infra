# Non-secret defaults only — secrets via `TF_VAR_*`. Set the real values
# before adding production resources (see environments/lab/terraform.tfvars
# for the lab pattern and environments/production/README.md for setup).
